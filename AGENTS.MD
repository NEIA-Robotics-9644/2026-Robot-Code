# AGENTS.md

## Build and Test

- This is a Java 17 WPILib/GradleRIO FRC robot project.
- Run `./gradlew build` to compile and test the robot code.
- Run `./gradlew spotlessApply` when formatting is needed.
- Do not deploy to the robot unless explicitly requested.